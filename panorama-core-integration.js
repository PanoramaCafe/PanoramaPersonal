/* Panorama Personal — sincronización local-first + diseño reloj restaurado */
(function(){
'use strict';
const cfg=window.PANORAMA_SUPABASE,STORE='panorama_cafe_personal_v1',PENDING='panorama_personal_pending_v2',DELETED='panorama_personal_deleted_v1',ROW='personal-main',PULL_MS=2500;
let syncing=false,applying=false,last='';
const clone=x=>x==null?x:JSON.parse(JSON.stringify(x));
function read(k=STORE){try{return JSON.parse(localStorage.getItem(k)||'null')}catch{return null}}
function write(x){localStorage.setItem(STORE,JSON.stringify(x));}
function same(a,b){try{return JSON.stringify(a)===JSON.stringify(b)}catch{return false}}
function status(t,k){let e=document.getElementById('panoramaSyncStatus');if(!e){e=document.createElement('div');e.id='panoramaSyncStatus';e.style.cssText='position:fixed;right:12px;bottom:12px;z-index:9999;padding:8px 11px;border-radius:999px;background:#fff;border:1px solid #ddd;font:800 11px system-ui';document.body.appendChild(e)}e.textContent=t;e.style.color=k==='ok'?'#16803a':k==='offline'?'#a56700':'#b36b00'}
function refresh(){if(!navigator.onLine)return status('● Sin conexión','offline');if(read(PENDING))return status('● Pendiente de sincronizar','pending');status('● Sincronizado','ok')}
function id(x){return x&&typeof x==='object'&&(x.id??x.sessionId??x.employeeId??x.key)}
function tomb(){try{return JSON.parse(localStorage.getItem(DELETED)||'{}')}catch{return {}}}
function saveTomb(x){localStorage.setItem(DELETED,JSON.stringify(x))}
function rememberDeletion(before,after){const out=tomb();for(const [key,oldVal] of Object.entries(before||{})){if(!Array.isArray(oldVal))continue;const now=new Set((Array.isArray(after?.[key])?after[key]:[]).map(id).filter(x=>x!=null).map(String));for(const item of oldVal){const k=id(item);if(k!=null&&!now.has(String(k))){out[key]=out[key]||{};out[key][String(k)]=Date.now()}}}saveTomb(out)}
function filterDeleted(data){if(!data||typeof data!=='object')return data;const d=clone(data),t=tomb();for(const [key,ids] of Object.entries(t)){if(Array.isArray(d[key]))d[key]=d[key].filter(x=>id(x)==null||!ids[String(id(x))])}return d}
function mergeValue(a,b,path='',deleted=tomb()){
 if(Array.isArray(a)||Array.isArray(b)){const aa=Array.isArray(a)?a:[],bb=Array.isArray(b)?b:[],out=[],map=new Map(),blocked=deleted[path]||{};for(const v of [...aa,...bb]){const k=id(v);if(k==null){out.push(clone(v));continue}if(blocked[String(k)])continue;const old=map.get(String(k));if(!old){map.set(String(k),clone(v));continue}const ot=new Date(old.updatedAt||old.updated_at||old.modifiedAt||old.createdAt||old.created_at||0).getTime(),nt=new Date(v.updatedAt||v.updated_at||v.modifiedAt||v.createdAt||v.created_at||0).getTime();map.set(String(k),nt>=ot?mergeValue(old,v,path,deleted):mergeValue(v,old,path,deleted));}return [...map.values(),...out]}
 if(a&&b&&typeof a==='object'&&typeof b==='object'){const o={...a};for(const k of Object.keys(b))o[k]=k in a?mergeValue(a[k],b[k],k,deleted):clone(b[k]);return o}
 return b===undefined?clone(a):clone(b)
}
function merge(local,remote){if(!local)return filterDeleted(remote);if(!remote)return filterDeleted(local);return filterDeleted(mergeValue(remote,local))}
function mark(x){localStorage.setItem(PENDING,JSON.stringify(x));refresh()}
function clear(){localStorage.removeItem(PENDING);refresh()}
function render(){try{if(typeof renderAll==='function')renderAll()}catch(e){console.warn(e)}}
function apply(x){if(!x||typeof x!=='object')return false;applying=true;try{db=x;write(x);last=JSON.stringify(x);render();window.dispatchEvent(new CustomEvent('panorama-core-personal-remote-update',{detail:{data:x}}));return true}finally{setTimeout(()=>applying=false,0)}}
function restoreClockLayout(){const reloj=document.getElementById('reloj'),clock=reloj?.querySelector('.clock-home'),working=document.getElementById('workingNowList')?.closest('.card');if(!reloj||!clock||!working)return;if(!reloj.querySelector('.clock-workspace')){const wrap=document.createElement('div');wrap.className='clock-workspace';reloj.insertBefore(wrap,clock);wrap.append(clock,working)}if(!document.getElementById('panorama-clock-layout')){const s=document.createElement('style');s.id='panorama-clock-layout';s.textContent='.clock-workspace{display:grid;grid-template-columns:minmax(360px,1fr) minmax(300px,.72fr);gap:14px;align-items:stretch;width:100%}.clock-workspace>.card{margin-top:0}.clock-workspace>.clock-home{max-width:none;margin:0}.clock-workspace .working-person{width:100%}.clock-workspace #workingNowList{display:flex;flex-direction:column;gap:8px}.clock-workspace .working-empty{min-height:180px;display:flex;align-items:center;justify-content:center}@media(max-width:900px){.clock-workspace{grid-template-columns:1fr}.clock-workspace>.card{margin-top:14px}}';document.head.appendChild(s)}}
restoreClockLayout();
if(!cfg?.url||!cfg?.key){refresh();return}
const H={apikey:cfg.key,Authorization:'Bearer '+cfg.key,'Content-Type':'application/json'};
async function getRemote(){const r=await fetch(cfg.url+'/rest/v1/panorama_personal_state?id=eq.'+encodeURIComponent(ROW)+'&select=data,updated_at',{headers:H});if(!r.ok)throw new Error(await r.text());const a=await r.json();return a[0]||null}
async function put(data){const r=await fetch(cfg.url+'/rest/v1/panorama_personal_state?on_conflict=id',{method:'POST',headers:{...H,Prefer:'resolution=merge-duplicates,return=representation'},body:JSON.stringify({id:ROW,data})});if(!r.ok)throw new Error(await r.text());const a=await r.json();return a[0]||null}
async function publishPayments(data){const em=new Map((data?.employees||[]).map(e=>[String(e.id),e]));for(const p of data?.payments||[]){if(!p?.id||!p?.employeeId||!Number.isFinite(Number(p.amount)))continue;const e=em.get(String(p.employeeId))||{};await fetch(cfg.url+'/rest/v1/panorama_payroll_payments?on_conflict=id',{method:'POST',headers:{...H,Prefer:'resolution=merge-duplicates,return=minimal'},body:JSON.stringify({id:String(p.id),source:'personal',employee_id:String(p.employeeId),employee_name:String(e.name||p.employeeName||''),amount:Number(p.amount),paid_date:String(p.paidDate||p.date||'').slice(0,10)||null,period_start:p.periodStart||null,period_end:p.periodEnd||null,note:p.note||'',account:p.account||null,updated_at:new Date().toISOString()})})}}
async function sync(force=false){if(syncing||applying||!navigator.onLine)return false;syncing=true;try{const local=read(),queued=read(PENDING),base=queued||local;if(!base){const row=await getRemote();if(row?.data)apply(filterDeleted(row.data));return true}const row=await getRemote(),combined=merge(base,row?.data||null);if(!same(combined,base)||force||queued){mark(combined);const saved=await put(combined),confirmed=filterDeleted(saved?.data||combined);apply(confirmed);try{await publishPayments(confirmed)}catch(e){console.warn('Pagos secundarios pendientes',e)}clear();last=JSON.stringify(confirmed);window.dispatchEvent(new Event('panorama-core-personal-synced'));return true}if(row?.data&&!same(filterDeleted(row.data),local))apply(filterDeleted(row.data));clear();return true}catch(e){const x=read(PENDING)||read();if(x)mark(x);console.warn('Panorama Personal: sincronización pendiente',e);return false}finally{syncing=false;refresh()}}
function observe(){let previous=read();setInterval(()=>{if(applying)return;const now=read();if(!same(now,previous)){rememberDeletion(previous,now);previous=clone(now);if(now){mark(now);setTimeout(()=>sync(),150)}}},500)}
window.addEventListener('online',()=>{refresh();sync(true)});window.addEventListener('offline',refresh);document.addEventListener('visibilitychange',()=>{if(!document.hidden)sync()});window.PanoramaPersonalSync={sync:()=>sync(true),status:()=>({online:navigator.onLine,pending:!!read(PENDING)}),merge};refresh();observe();sync();setInterval(()=>sync(),PULL_MS);
})();
